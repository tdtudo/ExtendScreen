请帮我设计并编写一个跨平台屏幕扩展软件，将安卓平板/手机作为电脑的第二屏幕使用。

## 1. 产品定位
一款**即开即用**的屏幕扩展工具。安卓端尽量**零操作**，用户的主要操作在电脑端完成。本次开发优先实现 **Windows电脑端 + 安卓移动端**。

## 2. 连接方式
### 2.1 Wi-Fi 无线连接（主要）
- 电脑端启动后，自动广播服务。
- 安卓端打开App后，**无需任何手动选择**，自动搜索并连接同一局域网内的电脑。若有多台电脑，才弹出列表让用户选择。
- 连接过程无需PIN码，或只在首次连接时弹窗确认一次。

### 2.2 USB 数据线连接（低延迟）
- 安卓设备通过USB连接电脑时，App应**自动检测并切换为有线模式**，无需用户手动开启USB调试以外的操作。
- 拔掉USB后，**自动切回Wi-Fi模式**，不中断使用。
- 系统自动优先使用USB传输画面。

## 3. 安卓端界面（极简）
安卓端App打开后只有三个状态界面：
1.  **连接中**：显示“正在连接电脑...”，带一个加载动画。
2.  **已连接**：显示电脑的扩展屏幕画面，全屏铺满，无任何按钮遮挡。
3.  **设置页**（从手机系统设置或通知栏入口进入，不在主界面）：仅包含分辨率选择、帧率选择、查看连接状态。

## 4. 核心功能
### 4.1 扩展模式
- 安卓设备作为电脑的**第二扩展屏幕**，不复制主屏幕。
- 用户可在电脑显示设置中拖拽排列两块屏幕的位置。
- 窗口和鼠标可在电脑屏幕和安卓屏幕之间自由穿梭。

### 4.2 触控回传
- 用户在安卓设备上的触摸操作，直接回传电脑端，模拟鼠标行为：
  - 单指滑动 = 鼠标移动
  - 双指滑动 = 页面滚动
  - 双指捏合 = 缩放（如适用）

### 4.3 显示调节
- 电脑端提供设置面板，可调整安卓端的分辨率和帧率。
- 安卓端自动适应横屏/竖屏，并将旋转状态同步给电脑。

## 5. 技术方案
- 通信：本地局域网TCP/UDP传输，USB使用ADB通道或TCP over USB
- 视频编码：H.264/H.265硬件编码，优先使用显卡硬件加速（如NVIDIA NVENC、Intel QuickSync、AMD VCE）
- 开发语言：
  - **电脑端（Windows）**：C++（Qt框架）或 C#（.NET/WPF）
  - **安卓端**：Kotlin（原生Android开发）
- 安卓端后台运行：App切换到后台或屏幕关闭后，应继续维持连接（可选项，由用户在设置中开启）

## 6. 界面风格
- 安卓端：纯黑背景，连接成功全屏无UI，极简无打扰。
- 电脑端：系统托盘图标 + 简洁设置面板，与Windows风格融合。
- 电脑端设置面板功能：
  - 显示已连接的安卓设备名称
  - 分辨率选择（自适应/720p/1080p/1440p）
  - 帧率选择（30fps/60fps）
  - Wi-Fi/USB连接方式显示
  - “断开此设备”按钮
## 7. 开发建议
- **第一阶段（MVP）**：实现同一Wi-Fi下的屏幕扩展 + 触控回传。
- **第二阶段**：加入USB有线连接。
- **第三阶段**：优化编码性能，降低延迟，加入帧率/分辨率调节。